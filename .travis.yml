sudo: false

language: node_js

notifications:
  email: false

node_js:
- stable

cache:
  directories:
  - node_modules

before_script:
  - npm install

script:
- npm run lint
- npm test

after_success:
- npm run release

branches:
  only:
  - master
